# Project Refactor Sweep

Status: in-progress
Triage: elevated — Phases 2-4 touch exit-strategy money logic (`exit_strategies/managers`, `broker.py`) and the orchestrator's control flow (`SignalOrchestrator`, `app/factory.py`-adjacent DI wiring), which spans multiple subsystems (exit strategies + trade execution + API composition root) even though each individual subphase is small.

## Goal

A full-repo sweep found: three unused backup files, a broken/dead second FastAPI entrypoint, a dead utility module explicitly marked for deletion, dead code inside the live endpoints module, a genuinely broken endpoint (calls a method that doesn't exist), two real bugs where exit-management code silently mis-reads position profit for dict-shaped positions (used in demo/backtest mode), a broker bug that never assigns tickets to simulated positions (breaking exit tracking in demo mode), and defensive orchestrator duck-typing that guesses at collaborator method names that don't actually exist on the real classes (i.e. dead fallback branches, not real flexibility). This plan fixes all of that, formalizes the orchestrator's real interfaces, makes the API layer testable via FastAPI dependency injection instead of importing live singletons, and backfills tests for the money-handling exit logic.

## Out of scope

- **`app/config/settings.py::Config` is not being split.** It's ~95 lines, already organized into commented `===` sections, and every call site reads it via `getattr(config, "NAME", default)`. Splitting it into multiple classes would touch every one of those call sites for no behavioral benefit at this project's size. Revisit only if/when distinct config concerns need independent lifecycles (e.g. per-symbol overrides), not as cleanup for its own sake.
- **Simulating real-time P&L for demo/backtest positions.** Phase 2 fixes the *ticket* bug so simulated positions can be tracked/closed, but `Broker._simulate_trade`/`_backtest_trade` still hardcode `"profit": 0.0` forever — nothing recalculates it as price moves. Making demo-mode profit realistic is a real feature (needs a price-driven mark-to-market loop), not a small bug fix. Left for a future plan.
- Any change to indicator math (MACD/RSI/SMA signal logic itself) — this sweep only touches orchestration, exit management, dead code, and API wiring.
- Adding CI (`.github/` currently has no workflow files) — flagged as a gap but not part of this plan.

## Phases

### Phase 1: Dead code and file hygiene (no behavior change)

#### Subphase 1.1: Remove stray backup files

- Change: delete `app/exit_strategies/managers/loss copy.py`, `app/exit_strategies/managers/profit copy.py`, `app/signals/indicators/sma_crossover copy.py`. (Confirmed via repo-wide grep that nothing imports any of these three paths — they are earlier drafts of the current `loss.py`/`profit.py`/`sma_crossover.py`.)
- Acceptance criteria (test-first): a test in `tests/test_repo_hygiene.py` asserts `Path("app/exit_strategies/managers/loss copy.py").exists()`, `Path("app/exit_strategies/managers/profit copy.py").exists()`, and `Path("app/signals/indicators/sma_crossover copy.py").exists()` are all `False`. No mocks needed (pure filesystem check).

#### Subphase 1.2: Remove the dead alternate entrypoint and its only dependency

- Change: delete `app/main3.py` (it imports `from utils.connection import ...` and `from data.market_data import ...` — top-level `utils`/`data` packages that do not exist in this project's layout, so it already fails to import as-is) and delete `app/utils/connection.py` (its only importer, confirmed via repo-wide grep, is `app/main3.py`). Add `app/__init__.py` (empty; every other `app/` subpackage already has one, `app/` itself was the sole outlier) and a root-level `conftest.py` (adds the repo root to `sys.path`) — both are strictly necessary for `import app.main` to work reliably from `tests/`, not part of the original bullet but required to make the second acceptance criterion testable at all.
- Acceptance criteria (test-first): test asserts `Path("app/main3.py").exists()` and `Path("app/utils/connection.py").exists()` are both `False`; a second test asserts `import app.main` still succeeds and `app.main.app` is a `fastapi.FastAPI` instance (the canonical entrypoint is unaffected), with `MetaTrader5.initialize` monkeypatched via the `mock_mt5` fixture so no real MT5 terminal is required.

#### Subphase 1.3: Remove dead `process_handling.py`

- Change: delete `app/utils/process_handling.py`. It is headed by a literal `### !!! TO BE DELETED !!! ###` comment, imports `from utils.configure_logging import logger` (a broken top-level import identical in kind to Subphase 1.2's), and repo-wide grep shows no other file imports anything from it.
- Acceptance criteria (test-first): test asserts `Path("app/utils/process_handling.py").exists()` is `False`; a second test greps all `*.py` files under `app/` for the substring `process_handling` and asserts zero matches remain.

#### Subphase 1.4: Remove dead commented-out endpoint block

- Change: in `app/routes/endpoints.py`, delete the triple-quoted commented-out block defining `backtest_signals_endpoint_historical` (references an undefined `BreakoutStrategy` name that exists nowhere in the codebase). Widened during implementation (pr-review finding): this was the only caller of `app.utils.backtest_signals.backtest_signals`, so also remove the now-unused `from app.utils.backtest_signals import backtest_signals` line from `endpoints.py` and delete the now-fully-dead `app/utils/backtest_signals.py` module itself.
- Acceptance criteria (test-first): test asserts the string `"BreakoutStrategy"` no longer appears anywhere in `app/routes/endpoints.py`'s source; a second test imports `app.routes.endpoints` and asserts `router.routes` still contains exactly the same 10 paths as before (`/status`, `/trading/start`, `/trading/stop`, `/signal/latest`, `/live_signal`, `/tick`, `/simulated_positions`, `/close_all`, `/test_historical`, `/stop_orchestrator`) — proving the deletion removed only dead comment text, not an active route.

#### Subphase 1.5: Remove unused `rm` import from endpoints.py

- Change: in `app/routes/endpoints.py`, remove `rm` from the `from app.factory import (...)` import block. It is imported but never referenced by any route handler in the file (only `br`, `md`, `trade_executor`, `orchestrators`, `signal_orchestrator`, and `Config` are actually used).
- Acceptance criteria (test-first): test asserts `import app.routes.endpoints` still succeeds; a second test reads the file's source and asserts a regex search for the standalone identifier `\brm\b` finds no matches.

#### Subphase 1.6: Move the root MT5 smoke-test script out of the pytest-lookalike name

- Change: create `scripts/` directory if it doesn't exist; move `test_mt5.py` (repo root) to `scripts/mt5_smoke_test.py` unchanged in content. Update its one reference in `CLAUDE.md` ("`test_mt5.py` at the repo root is a separate manual MT5 connectivity smoke script") to point at the new path.
- Acceptance criteria (test-first): test asserts `Path("test_mt5.py").exists()` is `False` and `Path("scripts/mt5_smoke_test.py").exists()` is `True`; a second test asserts no file matching `test_*.py` exists anywhere under the repo root outside of `tests/` and `scripts/` (walk the repo root's immediate children only, excluding `tests/`); a third test asserts the substring `test_mt5.py` no longer appears in `CLAUDE.md` and `scripts/mt5_smoke_test.py` does.

#### Subphase 1.7: Fix stale documentation references

- Change: `CLAUDE.md` and `README.md` both reference `app/signals/indicators/entry_filter.py`, which does not exist anywhere in `app/signals/indicators/` (that directory only contains `macd.py`, `rsi.py`, `sma_crossover.py`). In `CLAUDE.md`, remove `entry_filter.py` from the indicators list in the "Signal generation" paragraph. In `README.md`, change "SMA, MACD, RSI, entry filter calculations" to "SMA, MACD, RSI calculations" in the Project Structure section. Also update `CLAUDE.md`'s `app/main3.py` sentence (the "check which one is current" caveat) to state that `app/main3.py` was removed in this sweep and `app.main:app` is the sole entrypoint.
- Acceptance criteria (test-first): test asserts the substring `entry_filter.py` no longer appears in either `CLAUDE.md` or `README.md`; a second test asserts `CLAUDE.md` no longer contains the substring `app/main3.py is a separate/alternate entrypoint`.

### Phase 2: Fix confirmed exit/trade-logic bugs

#### Subphase 2.1: Fix the broken `/close_all` endpoint

- Change: `app/routes/endpoints.py::close_all_trades()` calls `trade_executor._close_all_trades()`, a method that does not exist anywhere in `app/trade_execution/trade_execution.py::TradeExecutor` (confirmed via repo-wide grep — it is defined nowhere). Add a new public method `close_all_trades(self)` to `TradeExecutor` that calls `self.broker.get_open_positions()`, and for each returned position calls `self.broker.close_position(ticket=pos_ticket(pos))` (import `pos_ticket` from `app.exit_strategies.exit_shared` so both dict and object positions work), skipping any position where `pos_ticket` returns `None`. Update the endpoint to call `trade_executor.close_all_trades()` instead of the nonexistent method.
- Acceptance criteria (test-first): test in `tests/trade_execution/test_trade_execution.py` using `mock_broker`: `mock_broker.get_open_positions.return_value` is set to `[make_position(as_dict=True, ticket=1), make_position(as_dict=True, ticket=2)]`; call `TradeExecutor(risk_manager=MagicMock(), broker=mock_broker, market_data=MagicMock()).close_all_trades()`; assert `mock_broker.close_position` was called exactly twice, once with `ticket=1` and once with `ticket=2`. A second test in `tests/routes/test_endpoints.py` uses a FastAPI `TestClient`, overrides `trade_executor` with a `MagicMock`, sends `POST /close_all`, and asserts the response status is `200` and `trade_executor.close_all_trades` was called exactly once (no `AttributeError`).

#### Subphase 2.2: Fix `is_break_even()` to support dict-shaped positions

- Change: `app/exit_strategies/exit_shared.py::is_break_even()` currently does `getattr(position, "profit", None)`, which returns `None` (defaulting to `0.0`, i.e. always reporting "break-even or better") for any dict-shaped position — dict positions have no `.profit` attribute. Replace the body with `profit = pos_profit(position)` (the dict/object-safe accessor already defined earlier in the same file), falling back to `0.0` only if `pos_profit` returns `None`.
- Acceptance criteria (test-first): test in `tests/exit_strategies/test_exit_shared.py` asserts `is_break_even({"profit": -5.0})` returns `False`; `is_break_even({"profit": 0.0})` returns `True`; `is_break_even(make_position(profit=-5.0))` (object form, via the `make_position` fixture) returns `False`; `is_break_even({})` (no profit key at all) returns `True` (preserves current "missing profit defaults to break-even" behavior).

#### Subphase 2.3: Fix `ProfitExitManager` to read profit via the dict/object-safe accessor

- Change: `app/exit_strategies/managers/profit.py::ProfitExitManager.check_exit_on_tick` — replace `profit = getattr(position, "profit", None)` with `profit = pos_profit(position)` (import `pos_profit` alongside the other `pos_*` imports already at the top of the file), keeping the existing `if profit is None: profit = 0.0` fallback line unchanged.
- Acceptance criteria (test-first): test in `tests/exit_strategies/managers/test_profit.py` builds a dict position via `make_position(as_dict=True, profit=-0.10)`, a `PosState` pre-armed at break-even (`be_armed=True`, `best_profit=0.0`, `ticks_seen=5`), and a tick; calls `ProfitExitManager(...).check_exit_on_tick(position, tick, state)`; asserts the return value is an `ExitAction` with `reason="trailing_breach_gt_5c"` — proving a dict position's real profit (`-0.10`, a $0.10 breach from `best_profit=0.0`, exceeding the $0.04 threshold) is now used instead of silently reading `0.0` and never breaching.

#### Subphase 2.4: Fix `LossExitManager` to read profit via the dict/object-safe accessor, and restore its arming-window safety net

- Change: `app/exit_strategies/managers/loss.py::LossExitManager.check_exit_on_tick` — replace `profit = getattr(position, "profit", None)` with `profit = pos_profit(position)` (add the import), keeping the `if profit is None: profit = 0.0` fallback.
- **Additional required change (found by pr-review during Subphase 2.2, not part of the original bullet):** with `is_break_even()` now honest (Subphase 2.2), a position that never reaches break-even within `be_arming_ticks` ticks falls out of block 1 (`if not state.be_armed and state.be_arming_ticks < be_arming_ticks:`) once the counter reaches the limit, never enters block 2 (`if state.be_armed:` — never true), and the method implicitly returns `None` on every subsequent tick regardless of how large the loss grows. The `failed_to_reach_be` exit that would have covered this is present in the file but commented out (lines ~78-89, inside a `"""..."""` block immediately after the `is_break_even` check in block 1). Uncomment and wire it back in as a real code path (not a docstring) so a position that exhausts the arming window without reaching break-even still exits, using the existing `reason="failed_to_reach_be"` and the same `_exit_action(...)` call signature already used elsewhere in this method. This was previously masked because `is_break_even()` always returned `True` for dict positions (the Subphase 2.2 bug), so `be_armed` was always set on tick 1 and this code path was unreachable — fixing 2.2 without also restoring this safety net would ship a real loss-of-protection regression the moment dict positions actually reach this manager (see Subphase 2.5's note on the current `lot`/`volume` key mismatch that keeps them from doing so today).
- Acceptance criteria (test-first): test in `tests/exit_strategies/managers/test_loss.py` builds a dict position via `make_position(as_dict=True, profit=-6.0)` and a fresh `PosState`; calls `LossExitManager(...).check_exit_on_tick(position, tick, state)`; asserts the return value is an `ExitAction` with `reason="profit_drop"` — proving the `-5` drop threshold now fires for dict positions (previously always read `profit=0.0` for dicts and could never cross the threshold). **Second required test:** drive a `PosState` through `be_arming_ticks` (default 20, via `Config.EXIT_BE_ARMING_TICKS`) consecutive `check_exit_on_tick` calls with a profit that never reaches break-even and never crosses the early `-5` drop threshold (e.g. `profit=-1.0` every tick) and never returns an exit action during that window; assert that the very next call (tick 21) returns an `ExitAction` with `reason="failed_to_reach_be"` — proving the safety net actually fires once the window is exhausted, not just that it doesn't crash.

#### Subphase 2.5: Assign tickets to simulated (demo/backtest) positions, and fix the `lot`/`volume` key mismatch that keeps them from reaching the exit managers at all

- Change: `app/trade_execution/broker.py::Broker.__init__` — add `self._next_sim_ticket = 1`. In `_simulate_trade` and `_backtest_trade`, add `"ticket": self._next_sim_ticket` to the constructed `trade` dict, and after appending to `self.open_positions_sim`, do `self._next_sim_ticket += 1`. (`close_position`'s demo/backtest branch already filters on `p.get("ticket") != ticket`, so it will now actually match something.)
- **Additional required change (found by pr-review during Subphase 2.2):** `_simulate_trade`/`_backtest_trade` key the constructed dict as `"lot"`, but `pos_volume()` (`app/exit_strategies/exit_shared.py`) looks for `("volume", "lots", "qty", "quantity")` — no match, so `pos_volume()` returns `None` for every simulated position, and both `LossExitManager.check_exit_on_tick` and `ProfitExitManager.check_exit_on_tick` bail immediately on their `volume is None` guard before ever reaching the profit logic Subphases 2.2-2.4 fix. Without this, dict-shaped simulated positions never reach the exit managers at all, regardless of how correct the managers' own logic is — the Subphase 2.2/2.3/2.4 fixes remain provably inert in demo/backtest mode until this key is fixed too. Rename the key from `"lot"` to `"volume"` in both `_simulate_trade` and `_backtest_trade` (check for any other reader of the `"lot"` key on these dicts first via repo-wide grep before renaming, to avoid breaking something else that currently reads it).
- Acceptance criteria (test-first): test in `tests/trade_execution/test_broker.py`, using `mock_mt5` (so `Broker(TradingMode.DEMO)` doesn't need a real terminal) and monkeypatching `mt5.symbol_info_tick` to return a `make_tick()`: call `broker.place_buy("EURUSD", 0.01, None, None)` twice; assert `broker.open_positions_sim[0]["ticket"] != broker.open_positions_sim[1]["ticket"]` and neither is `None`; call `broker.close_position(ticket=broker.open_positions_sim[0]["ticket"])`; assert `len(broker.open_positions_sim) == 1` and the remaining entry's ticket equals the second placed trade's ticket. **Second required test:** after placing a simulated trade, assert `app.exit_strategies.exit_shared.pos_volume(broker.open_positions_sim[0])` returns the real placed volume (not `None`) — proving a simulated position, as actually constructed by the broker, can now be read by the exit managers' `volume is None` guard rather than being silently skipped.

### Phase 3: Formalize the SignalOrchestrator's collaborator interfaces

#### Subphase 3.1: Call `EnterTrade`'s real method instead of guessing at nonexistent names

- Change: `app/services/trade_services.py::SignalOrchestrator` — in both `_on_tick` and `_run_entries`, the code does `fn = getattr(self.enter_trade, "on_signal", None) or getattr(self.enter_trade, "execute", None) or getattr(self.enter_trade, "enter", None)`. `EnterTrade` (`app/trade_execution/helpers/prepare_trade.py`) defines none of these — only `enter_trade(self, signal, account_balance)` — so this branch is unreachable dead code today. Replace both occurrences with a direct call: `if self.enter_trade is not None:` then `account_balance = 0.0`; `get_bal = getattr(self.broker, "get_account_balance", None)`; `if callable(get_bal):` try `account_balance = float(get_bal())` inside a `try/except Exception: pass`; then call `self.enter_trade.enter_trade(sig, account_balance)` inside the existing `try/except Exception as exc:` logging block.
- Acceptance criteria (test-first): test in `tests/services/test_trade_services.py` constructs `SignalOrchestrator(collector=MagicMock(), signal_generator=MagicMock(), trading_service=None, enter_trade=MagicMock(spec=["enter_trade"]), broker=None)`; configures the mock `signal_generator.generate_signal.return_value = {"symbol": "EURUSD", "final_signal": "buy", "pullback_completed": True}`; calls `orchestrator._run_entries(snapshot={}, asof=datetime.now(timezone.utc))`; asserts `enter_trade.enter_trade.assert_called_once()` with the signal dict as the first positional argument — proving this path is now reachable (previously it was dead code since none of the guessed attribute names existed on `EnterTrade`).

#### Subphase 3.2: Call the signal generator's one real method directly

- Change: `app/services/trade_services.py::SignalOrchestrator._run_entries` — `gen = getattr(sg, "generate_signal", None) or getattr(sg, "generate_signals", None) or getattr(sg, "__call__", None)` followed by a `TypeError`-fallback ladder trying `gen(snapshot)`, `gen(candles_snapshot=snapshot)`, `gen()`, `gen(account_balance=bal)`. All three current signal-generator implementations (`StrongSignalStrategy`, `MultiTimeframeStrongSignalStrategy`, `NTickConfirmedSignalStrategy` in `app/signals/strategies/`) implement only `generate_signal(self, candles, ...)` taking the candles/snapshot as the first positional argument — none implement `generate_signals` or rely on `__call__`, and none accept `candles_snapshot=`, `()`, or `account_balance=` calling conventions. Replace the lookup-and-fallback ladder with a direct `sig_out = sg.generate_signal(snapshot)` call inside the existing `try/except Exception as exc:` block (keep the existing exception logging and early-return-on-failure behavior; just remove the multi-name lookup and the three `TypeError` fallback attempts).
- Acceptance criteria (test-first): test asserts `SignalOrchestrator._run_entries` calls `signal_generator.generate_signal` exactly once with `snapshot` as its sole positional argument, using a `MagicMock` signal_generator returning `{"symbol": "EURUSD", "final_signal": "hold"}`. A second, parametrized test instantiates each of `StrongSignalStrategy`, `MultiTimeframeStrongSignalStrategy`, and `NTickConfirmedSignalStrategy` with minimal constructor args and a stub `indicators`/`base` dependency, wraps each in a `SignalOrchestrator`, and asserts calling `_run_entries` with a small candles snapshot completes without raising — proving the simplified direct call still works for every real implementation.

#### Subphase 3.3: Log (don't silently swallow) exit-execution failures

- Change: `app/services/trade_services.py::SignalOrchestrator._execute_exit_actions` — the two `except TypeError: try: closer(...) except Exception: pass` / `except Exception: pass` blocks around the `closer(...)` call silently discard all errors. Replace both bare `except Exception: pass` blocks with `except Exception as exc: self._log_exception(f"[Orchestrator] broker close failed for ticket={ticket} symbol={symbol}: {exc!r}")`, reusing the `_log_exception` helper already defined on the class.
- Acceptance criteria (test-first): test constructs `SignalOrchestrator(collector=MagicMock(), signal_generator=MagicMock(), broker=MagicMock(), logger=MagicMock())` where `broker.close_position.side_effect = RuntimeError("boom")` and `broker.close_trade`/`close_order` are absent (so `close_position` is the one selected); calls `orchestrator._execute_exit_actions([{"ticket": 1, "symbol": "EURUSD", "side": "sell", "volume": 0.01}])`; asserts `orchestrator.logger.exception.called` is `True` — proving the failure is now surfaced instead of silently discarded.

### Phase 4: Composition-root testability for the API layer

#### Subphase 4.1: Add getter functions for factory singletons

- Change: `app/factory.py` — after the existing module-level construction of `md`, `br`, `trade_executor`, `orchestrators` (no change to when/how these are built — they stay eagerly constructed at import time in `Mode.LIVE`, exactly as today), add four plain functions at the end of the file: `def get_market_data(): return md`, `def get_broker(): return br`, `def get_trade_executor(): return trade_executor`, `def get_orchestrators(): return orchestrators`.
- Acceptance criteria (test-first): test asserts `app.factory.get_market_data() is app.factory.md`, `app.factory.get_broker() is app.factory.br`, `app.factory.get_trade_executor() is app.factory.trade_executor`, and `app.factory.get_orchestrators() is app.factory.orchestrators` — four identity checks proving each getter returns the exact existing singleton, not a copy.

#### Subphase 4.2: Wire endpoints.py through FastAPI `Depends()`

- Change: `app/routes/endpoints.py` — remove the direct `from app.factory import (orchestrators, signal_orchestrator, trade_executor, br, md)` import (per Subphase 1.5, `rm` is already gone). Instead `from app.factory import get_orchestrators, get_broker, get_market_data, get_trade_executor` and `from fastapi import APIRouter, Depends`. Give every route handler the collaborators it actually uses as `Depends(...)`-injected parameters instead of reading the module-level names, e.g. `def get_status(orchestrators: dict = Depends(get_orchestrators), trade_executor = Depends(get_trade_executor)):`. Replace `get_any_orchestrator()`'s body with `return next(iter(orchestrators.values()))` taking `orchestrators` as a parameter (or inline this logic at each of its two call sites) instead of referencing the module-level `signal_orchestrator` name. Every route (`/status`, `/trading/start`, `/trading/stop`, `/signal/latest`, `/live_signal`, `/tick`, `/simulated_positions`, `/close_all`, `/test_historical`, `/stop_orchestrator`) must obtain `orchestrators`/`br`/`md`/`trade_executor` only via its `Depends(...)` parameters, never via a module-level import.
- Acceptance criteria (test-first): test builds `app = FastAPI(); app.include_router(router)`, sets `app.dependency_overrides[get_trade_executor] = lambda: mock_trade_executor` and `app.dependency_overrides[get_orchestrators] = lambda: {"EURUSD": mock_orchestrator}` (both `MagicMock`s, `mock_orchestrator.is_running.return_value = True`), issues `GET /status` via `TestClient(app)`, and asserts the response is `200` with JSON `{"orchestrator_running": {"EURUSD": True}, ...}` reflecting the mocked orchestrator — proving `/status` no longer needs the real `app.factory` singletons to be exercised.

#### Subphase 4.3: Prove the whole router is testable without touching MT5

- Change: none (test-only). Add `tests/routes/test_endpoints.py` (extending the file from Subphase 2.1/4.2 if already created) covering the remaining routes not yet covered.
- Acceptance criteria (test-first): a single test monkeypatches `MetaTrader5.initialize` to raise `AssertionError("MT5 should not be touched in this test")`, overrides `get_broker`, `get_market_data`, `get_trade_executor`, and `get_orchestrators` with `MagicMock`-backed lambdas (configuring return values so each route's logic succeeds, e.g. `mock_broker.mode = TradingMode.DEMO`, `mock_broker.open_positions_sim = []`, `mock_md.get_historical_candles.return_value = []`), then drives all ten routes via `TestClient` and asserts every response status is in `range(200, 300)` — proving the full router is exercisable through dependency overrides alone, with zero real MT5/broker/orchestrator construction.

### Phase 5: Backfill tests for exit-strategy logic

#### Subphase 5.1: Cover `exit_shared.py`'s accessors for both dict and object positions

- Change: none (test-only). `tests/exit_strategies/test_exit_shared.py` (extend the file created in Subphase 2.2).
- Acceptance criteria (test-first): a parametrized test covers `pos_symbol`, `pos_side` (both numeric `type=0`/`type=1` and string `"buy"`/`"sell"`/`"long"`/`"short"`/an unrecognized string), `pos_ticket`, `pos_entry`, `pos_volume`, and `pos_profit`, each run twice — once against a dict position (`make_position(as_dict=True, ...)`) and once against an object position (`make_position(as_dict=False, ...)`) — asserting identical, correct results from both shapes; plus one case per accessor asserting `None` is returned when the relevant field is entirely absent.

#### Subphase 5.2: Cover `ExitTrade.on_tick`'s cooldown gate

- Change: none (test-only). New file `tests/exit_strategies/test_exit_trade.py`.
- Acceptance criteria (test-first): construct `ExitTrade(broker=mock_broker, risk_manager=MagicMock())` where `mock_broker.get_open_positions.return_value = [make_position(as_dict=True, ticket=1, profit=-6.0)]`; call `exit_trade.on_tick(make_tick())` twice back-to-back (same test, no sleep); assert the first call's return list has length `1` with `reason="profit_drop"`, and the second call (still within `EXIT_COOLDOWN_SECONDS`) returns `[]` — proving `_should_exit`'s per-ticket cooldown suppresses a second exit action on the very next tick.

#### Subphase 5.3: Cover `ExitTrade.on_candle_close`'s HTF gating

- Change: none (test-only). `tests/exit_strategies/test_exit_trade.py` (extend from 5.2).
- Acceptance criteria (test-first): construct `ExitTrade` with `ExitTradeConfig(htf_filter_enabled=True, profit_exits_on_candle_close=True, profit_exits_on_tick=False)`; set up a break-even-armed buy position and state such that `ProfitExitManager.check_exit_on_candle_close` would otherwise return an exit action; call `exit_trade.update_bias("EURUSD", m15="buy")` then `exit_trade.on_candle_close(symbol="EURUSD", close_price=1.1005)` and assert the result is `[]` (HTF bias `m15="buy"` supports an existing buy position, so the profit exit is gated off); then call `exit_trade.update_bias("EURUSD", m15="sell")` and assert `on_candle_close(...)` now returns a non-empty list (HTF bias opposes the position, so the exit is allowed through).

#### Subphase 5.4: Cover `ProfitExitManager`'s break-even arming path

- Change: none (test-only). `tests/exit_strategies/managers/test_profit.py` (extend from Subphase 2.3).
- Acceptance criteria (test-first): test asserts `check_exit_on_tick` returns `None` and does not set `state.be_armed = True` while `is_break_even(position)` is `False` (position still at a loss, arming not yet reached); a second call with the same state but `is_break_even(position)` now `True` (profit `>= 0`) asserts `state.be_armed` becomes `True` and the method returns `None` for that call (arming happens without an immediate exit).

#### Subphase 5.5: Cover `LossExitManager`'s "recovered after unprofitable" exit path

- Change: none (test-only). `tests/exit_strategies/managers/test_loss.py` (extend from Subphase 2.4).
- Acceptance criteria (test-first): drive one `PosState` through three sequential `check_exit_on_tick` calls with profit values `0.0` (arms BE), `-1.0` (sets `was_unprofitable_after_be=True`, returns `None` since `-1.0 > drop_profit_after_be=-5`), then `0.02` (`0.0 < 0.02 < 0.05`); assert the third call returns an `ExitAction` with `reason="be_recovered_after_unprofit"`.

## Open questions

- `app/main3.py` (deleted in Subphase 1.2) is the only place in the codebase implementing a `/ws/symbol_data` WebSocket endpoint and a `/account_info` route — neither exists in `app/main.py`/`app/routes/endpoints.py`. The file is already non-functional (broken imports), so nothing currently works either way, but confirm whether that WebSocket/account-info functionality is still wanted before or after this deletion lands; if so, it should be ported into `app/routes/endpoints.py` as its own follow-up plan rather than folded into this cleanup sweep.
- This plan does not add CI (no workflow files exist under `.github/` today). Once Phase 5's test suite exists, consider a separate small plan to add a GitHub Actions workflow running `pipenv run pytest` on push/PR.
- **Pre-existing bug found by pr-review during Subphase 2.2, outside this plan's original scope:** `app/exit_strategies/exit_trade.py::ExitTrade.on_candle_close` calls `self._profit_manager.check_exit_on_candle_close(...)`, but `ProfitExitManager` (`app/exit_strategies/managers/profit.py`) only defines `check_exit_on_tick` — `check_exit_on_candle_close` does not exist anywhere, so this call would raise `AttributeError`. Currently dormant: it's gated behind `profit_exits_on_candle_close`, which defaults to `False` (`Config.EXIT_PROFIT_EXITS_ON_CANDLE_CLOSE` isn't even defined in `Config`, so the `getattr(..., False)` fallback applies) — so this path isn't exercised under default config. However, Subphase 5.3 as currently written is test-only and its acceptance criteria assume `ProfitExitManager.check_exit_on_candle_close` already exists and works. **Before Phase 5 reaches Subphase 5.3, decide and amend the plan:** either (a) widen 5.3 to implement `check_exit_on_candle_close` on `ProfitExitManager` for real (it would need broadly the same profit-reading/threshold logic as `check_exit_on_tick`, adapted for a candle close price rather than a tick), or (b) if candle-close profit exits are not actually a wanted feature, remove the dead call in `exit_trade.py` and rescope 5.3 accordingly. Don't let 5.3 land as "test-only" against a method that doesn't exist.
- **Threshold-scale mismatch between `ProfitExitManager` and `LossExitManager`, found by pr-review during Subphase 2.3 — a strategy decision, not a bug fix, so deliberately not folded into any subphase.** The two managers share the same `PosState` per ticket (confirmed in `ExitTrade.on_tick`: loss is checked first every tick, then profit if no loss action fired), but operate at wildly different scales: `profit.py`'s trailing-exit guard requires `profit > 0.00` and exits on a `$0.04` breach from peak; `loss.py`'s post-BE drop exit only fires at `-$5`. A position that gives back from `+$0.10` to `-$0.02` falls out of `profit.py`'s guard (profit no longer positive) but is nowhere near `loss.py`'s `-$5` threshold — so that specific small give-back has **no immediate exit trigger in either manager**, even after Subphases 2.2-2.4 all land; it just gets tracked as "unprofitable after BE" until it either recovers to `0 < profit < 0.05` or drops all the way to `-$5`. Decide (before or after this plan reaches `docs/plans/done/`, doesn't block Phase 2): should `loss.py`'s post-BE drop threshold be tightened to be closer in scale to `profit.py`'s $0.04, should `profit.py`'s trailing logic be extended to also cover the negative-crossing case, or is this gap acceptable as-is given the position sizes/pip values this bot actually trades? Whatever is decided, it's a values/strategy call, not a mechanical correctness fix like the rest of Phase 2.
- **Pre-existing config-wiring bug, found by pr-review during Subphase 2.4 — deserves its own future subphase, not folded into this plan's remaining scope.** `LossExitManager` reads `getattr(self.config, "EXIT_BE_ARMING_TICKS", 20)` and `ProfitExitManager` reads `getattr(self.config, "be_distance_pips", 3.0)`, but the `config` object actually passed to both in production is `ExitTradeConfig` (an all-lowercase-field dataclass defined in `exit_trade.py`), which has neither `EXIT_BE_ARMING_TICKS` nor a matching field for the profit-manager lookup — so the hardcoded fallback values (`20` and `3.0`) always win, regardless of what `app/config/settings.py` actually configures (`EXIT_BE_ARMING_TICKS = 90`, `EXIT_BE_DISTANCE_PIPS = 0.5`). Before Subphase 2.4, this was inert for the arming-ticks case (the counter only gated a check that was itself duplicated elsewhere); after 2.4, it directly controls when the newly-restored `failed_to_reach_be` safety net force-closes a position — at a 4.5x shorter window than the operator configured. Recommend a small follow-up subphase: wire `ExitTradeConfig`'s construction to actually pull from `Config.EXIT_BE_ARMING_TICKS`/`Config.EXIT_BE_DISTANCE_PIPS` (or rename its fields to match, whichever direction makes more sense given `ExitTradeConfig`'s other fields), and add a `be_arming_ticks > 0` guard when doing so — with the real config wired through, setting the value to `0` as a natural way to "disable the arming window" would otherwise force-close every position on tick 1 under the current (Subphase 2.4) code structure.

## QA

### 2026-09-12 — Subphase 1.1 (Remove stray backup files)

Checked: Subphase 1.1 only.

- `pipenv run pytest tests/test_repo_hygiene.py -v` → 1 passed.
- `pipenv run pytest -q` (full suite) → 1 passed, 1 collected — `tests/test_repo_hygiene.py` is currently the only test file in the repo, so this is expected at this point in the plan, not a sign of skipped coverage.
- Confirmed via filesystem listing that all three files are gone: `app/exit_strategies/managers/loss copy.py`, `app/exit_strategies/managers/profit copy.py`, `app/signals/indicators/sma_crossover copy.py`. No `__pycache__` remnants of them either.
- `tests/test_repo_hygiene.py::test_backup_files_removed` asserts `not Path(...).exists()` for all three exact paths named in the acceptance criteria — matches the criteria, not a weaker check (e.g. doesn't just check a directory listing count).
- Repo-wide grep for the three path substrings (`loss copy`, `profit copy`, `sma_crossover copy`) turns up three hits: the plan file itself (expected — describes the deletion), `tests/test_repo_hygiene.py` (expected — the assertion), and **`CLAUDE.md` line 40**, which still contains: "Note: `managers/loss copy.py`, `managers/profit copy.py`, and `indicators/sma_crossover copy.py` are stray backup files, not part of the active import graph." This sentence is now stale/inaccurate — it describes files that no longer exist as if they're still present in the repo.

**Verdict: gap found.** Test and deletion both satisfy the stated acceptance criteria and nothing else broke, but the QA check "confirm nothing else in the repo still references any of those three file paths" surfaced a real miss: `CLAUDE.md`'s "Exit strategies" paragraph still documents the three now-deleted files as existing stray backups. This isn't covered by any subphase's stated acceptance criteria (Subphase 1.7 only targets the `entry_filter.py` and `main3.py` stale references), so it's a plan gap, not a criteria failure — recommend either folding a one-line CLAUDE.md fix into Subphase 1.1 (or Subphase 1.7) before this goes to `docs/plans/done/`, so documentation doesn't drift out of sync with the codebase it describes.

Not a blocker for proceeding to Subphase 1.2 — the stray CLAUDE.md line doesn't affect behavior or test coverage — but should be resolved before Phase 1 as a whole is marked done.

**Resolved in this same branch (Subphase 1.1):** removed the stale sentence from CLAUDE.md's Exit strategies paragraph. Also fixed a pr-reviewer finding: `tests/test_repo_hygiene.py`'s assertions were cwd-relative and would pass vacuously if pytest were ever run from outside the repo root; anchored them to `Path(__file__).resolve().parents[1]` instead, since Subphases 1.2/1.3/1.6 extend this same file with more path assertions.

### 2026-09-12 — Subphase 1.2 (Remove the dead alternate entrypoint and its only dependency)

Checked: Subphase 1.2 only.

- `pipenv run pytest -q` (full suite) → 4 passed (2 in `tests/test_repo_hygiene.py`, 2 in new `tests/test_main.py`). `--collect-only` confirms exactly these 4, nothing extra picked up.
- Filesystem check: `app/main3.py` and `app/utils/connection.py` are both gone.
- Repo-wide grep for `main3` / `utils.connection` / `utils/connection` turns up only expected hits: `tests/test_repo_hygiene.py` (the assertion), the plan file itself, and **`CLAUDE.md` line 48** ("`app/main3.py` is a separate/alternate entrypoint — check which one is current..."). Unlike the stray CLAUDE.md miss flagged in Subphase 1.1's QA, this one is **not a new gap** — Subphase 1.7's stated Change bullet already explicitly targets this exact sentence for update. No other code, config, or script references either deleted file.
- `tests/test_repo_hygiene.py::test_dead_alternate_entrypoint_removed` asserts `not Path(...).exists()` for both exact paths named in the acceptance criteria — matches criteria precisely.
- `tests/test_main.py` has two tests (`test_main_import_succeeds`, `test_main_app_is_fastapi_instance`) rather than one bundled "second test" — a finer-grained split of the same acceptance criterion, not a weaker check. Both take the `mock_mt5` fixture. Verified `mock_mt5` is genuinely load-bearing, not decorative: manually reproduced (`pipenv run python -c "import app.main"` with no mocking) that the import chain `app.main` → `app.routes.endpoints` → `app.factory` eagerly constructs `Broker(Mode.LIVE)` at module-import time, whose `__init__` calls `mt5.initialize()` and raises `RuntimeError("MT5 initialization failed")` if that returns falsy — this is exactly what happens without a real MT5 terminal. `tests/conftest.py::mock_mt5` monkeypatches the real `MetaTrader5` module's `initialize`/`shutdown`/etc. attributes directly (not a substitute module), so `app.factory`'s `import MetaTrader5 as mt5` sees the patched functions too. `isinstance(app.main.app, FastAPI)` correctly checks against the real `fastapi.FastAPI` class, and `app/main.py` confirms `app = FastAPI(lifespan=lifespan)` is exactly that.
- Confirmed `app.main.app` is only reachable as a `FastAPI` instance if MT5 is mocked — no test here makes a real MT5/broker call.

**Assessment of the added `app/__init__.py` and root `conftest.py` (not in the subphase's stated Change bullet):**

Empirically verified both are minimal, load-bearing necessities, not scope creep:
- Temporarily removed root `conftest.py` and re-ran `tests/test_main.py`: both tests failed with `ModuleNotFoundError: No module named 'app'`. `pytest.ini` only sets `testpaths = tests`; it does not add the repo root to `sys.path`, and `pipenv run pytest` (unlike `python -m pytest`) does not implicitly add the current working directory either. Restoring `conftest.py` (which does `sys.path.insert(0, project_root)`) fixed both tests. This file is genuinely required for `import app.main` to work from `tests/`, and its scope is minimal (12 lines, one job).
- Temporarily removed `app/__init__.py` and re-ran the same tests: they still passed (Python 3.3+ implicit namespace packages don't strictly require `__init__.py` once the root is on `sys.path`). So this file is not strictly load-bearing for import mechanics, but it is a reasonable, minimal consistency fix: every other subpackage under `app/` (`config`, `data`, `exit_strategies`, `risk`, `routes`, `services`, `signals`, `trade_execution`, `utils`) already has an `__init__.py` — `app/` itself was the one outlier, an accidental gap rather than an intentional namespace-package choice. Making it a regular package removes ambiguity for tooling and avoids relying on namespace-package semantics that nothing else in this repo uses.
- Checked for side effects on test collection: `pytest.ini`'s `testpaths = tests` keeps collection scoped to `tests/` regardless of the new root `conftest.py` (pytest always loads root conftests for fixture availability, but that's independent of what gets collected). `--collect-only` confirms only the expected 4 tests are picked up; the root-level `test_mt5.py` (not yet moved — that's Subphase 1.6) is not accidentally swept into collection.
- Verdict on this addition: justified minimal necessity, not unwarranted scope creep. `conftest.py` is strictly required; `app/__init__.py` is a small, correct consistency fix with no observed side effects. Recommend the plan's Subphase 1.2 Change bullet be amended to mention both files explicitly so the written plan matches what was actually needed, but this is a documentation nit, not a functional gap.

**Verdict: proceed.** All stated acceptance criteria are met by tests that assert the specific behavior described (not weaker), `mock_mt5` is genuinely exercised, and no test touches real MT5. The one remaining stale CLAUDE.md reference to `main3.py` is already explicitly in Subphase 1.7's scope. Ready for the developer to continue to Subphase 1.3.

**pr-reviewer follow-up, resolved in this same branch:** flagged that `mock_mt5` is a function-scoped fixture, but `app.factory` calls real `mt5.initialize()` at *module import time* (via eager `Broker(Mode.LIVE)` construction) — so any future test file that does a top-level `from app.routes.endpoints import router` (planned for Subphases 2.1/4.2) would trigger that real call during pytest collection, before any fixture runs, regardless of which test asks for `mock_mt5`. Fixed proactively: added an unconditional baseline patch at the top of `tests/conftest.py` (outside any fixture, so it runs at conftest import time, which pytest always does before collecting test modules in the directory) that stubs `mt5.initialize`/`shutdown`/`symbol_select`/`symbol_info_tick`/`positions_get`/`last_error` to safe defaults. The per-test `mock_mt5` fixture still exists unchanged for tests that want to assert on calls or customize return values. Also removed an unused `pytest` import in `tests/test_main.py` (pr-reviewer nit) and amended this subphase's Change bullet to mention `app/__init__.py`/root `conftest.py` (documentation nit both QA and pr-reviewer raised).

### 2026-09-12 — Subphase 1.3 (Remove dead `process_handling.py`)

Checked: Subphase 1.3 only.

- `pipenv run pytest -q` (full suite) → 6 passed (the prior 4 in `tests/test_repo_hygiene.py` plus 2 new ones there — `test_dead_process_handling_removed`, `test_no_process_handling_references` — plus the 2 in `tests/test_main.py`; matches the plan's running total exactly).
- Filesystem check: `app/utils/process_handling.py` is gone (confirmed via directory listing of `app/utils/` and a repo-wide filename search for `*process_handling*`, including `__pycache__` — no artifacts of any kind remain).
- Repo-wide case-insensitive grep for the substring `process_handling` (not scoped to `app/`) turns up exactly two hits: `tests/test_repo_hygiene.py` (the new assertions) and this plan file itself (describing the deletion). Nothing else in the repo — no other source file, config, or doc (including `CLAUDE.md`, which was checked given Subphase 1.1's QA caught a stale reference of this same shape) — mentions `process_handling`. No new documentation-drift gap here, unlike Subphase 1.1.
- `tests/test_repo_hygiene.py::test_dead_process_handling_removed` asserts `not (REPO_ROOT / "app/utils/process_handling.py").exists()` — matches the first acceptance criterion exactly (path anchored to `REPO_ROOT`, consistent with the fix applied to this file back in Subphase 1.1's QA pass, so it isn't cwd-relative).
- `tests/test_repo_hygiene.py::test_no_process_handling_references` walks `app_dir.rglob("*.py")`, reads each file's text, and asserts none contain the substring `process_handling` — matches the second acceptance criterion exactly ("greps all `*.py` files under `app/` for the substring `process_handling` and asserts zero matches remain"), including a `try/except` around file reads that doesn't mask real failures (a decode error just skips that file rather than crashing, consistent with the other repo-hygiene tests' style).
- No test in this subphase calls real MT5 or a broker — both tests are pure filesystem/text checks.

**Verdict: proceed.** Both stated acceptance criteria are met by tests that assert the specific behavior described (exact path absence; zero substring matches under `app/`), the deletion is clean with no stray references anywhere in the repo (including docs), and the full suite is green at the expected count of 6. Ready for the developer to continue to Subphase 1.4.

**pr-reviewer follow-up, resolved in this same branch:** `test_no_process_handling_references`'s bare `except Exception: pass` around the file read could silently skip an unreadable file rather than fail the test — since `read_text(errors="ignore")` already precludes decode errors, the only things it could catch are I/O errors (permissions, locked file), which should make the guard fail loudly, not pass silently. Narrowed to `except OSError` and now records the unreadable path as a match (failing the test) instead of swallowing it. QA's earlier note that the try/except handles "decode errors" was inaccurate for the same reason and is superseded by this fix.

### 2026-09-12 — Subphase 1.4 (Remove dead commented-out endpoint block)

Checked: Subphase 1.4 only.

- `pipenv run pytest -q` (full suite) → 8 passed (the prior 6 plus 2 new ones in `tests/routes/test_endpoints.py`: `test_breakout_strategy_removed`, `test_router_has_exactly_ten_active_routes`). `--collect-only` confirms exactly these 8, nothing extra picked up.
- `git diff app/routes/endpoints.py` shows a clean, surgical removal: only the `"""..."""`-wrapped `backtest_signals_endpoint_historical` block (between `test_historical()` and `stop_orchestrator()`) is gone. Nothing else in the file changed.
- Repo-wide grep for `BreakoutStrategy` → 2 hits, both expected: `tests/routes/test_endpoints.py` (the assertion) and this plan file itself. Zero hits anywhere in `app/`. Repo-wide grep for `backtest_signals_endpoint_historical` → 1 hit, in the new test's docstring/message text (expected, describes what was removed) — no code reference remains.
- `test_breakout_strategy_removed` reads the file's live source text and asserts the exact substring `"BreakoutStrategy"` is absent — matches the first acceptance criterion precisely (not a weaker "route still works" check).
- `test_router_has_exactly_ten_active_routes` imports `app.routes.endpoints.router`, builds `{route.path for route in router.routes}`, and asserts set-equality (not just length) against the exact 10 paths named in the plan's acceptance criteria — stronger than a bare count, since it would also catch a wrong path substituted for a correct one. Independently re-ran via `python -c` outside pytest: exactly 10 paths, matching the plan's list exactly (`/status`, `/trading/start`, `/trading/stop`, `/signal/latest`, `/live_signal`, `/tick`, `/simulated_positions`, `/close_all`, `/test_historical`, `/stop_orchestrator`).
- **MT5-safety check (the important one, since `tests/routes/test_endpoints.py` is the first test file to import `app.routes.endpoints` at all):** the `from app.routes.endpoints import router` statement is actually **function-scoped** — indented inside `test_router_has_exactly_ten_active_routes(mock_mt5)`, not at true module level — so it executes at test-call time, after the `mock_mt5` fixture has already monkeypatched `mt5.initialize`/etc., not at collection time. This test is therefore double-protected (per-test `mock_mt5` fixture, plus the conftest baseline patch) and was never actually exercising the collection-time hazard Subphase 1.2's QA originally flagged. `test_breakout_strategy_removed` doesn't import the module at all (pure text read via `pathlib`), so no MT5 exposure there either.
- Verified the conftest baseline patch (added in Subphase 1.2, applied unconditionally at `tests/conftest.py` import time) is genuinely load-bearing and working — rather than assuming it — by empirically reproducing the actual collection-time hazard it exists to prevent: created a temporary probe file (`tests/test_zz_temp_module_level_import.py`) with a **true** module-level `from app.routes.endpoints import router` (executed at collection time, no fixture involved). With the baseline patch's body temporarily commented out in `tests/conftest.py`, running this probe failed collection with `RuntimeError: MT5 initialization failed` (traceback: `app.routes.endpoints` → `app.factory` → `create_broker(Mode.LIVE)` → `Broker.__init__` → real `mt5.initialize()` returning falsy) — reproducing exactly the hazard described. Restoring the baseline patch (no other change) made the same probe pass (1 passed) and the full suite pass at 9 (8 + probe). Deleted the probe file afterward and confirmed `git diff tests/conftest.py` is empty (round-tripped back to its original content) and the full suite is back to 8 passed.
- No test in this subphase calls real MT5, a real broker, or a real orchestrator — confirmed both by static reading and by the collection-time probe above.

**Verdict: proceed.** Both stated acceptance criteria are met by tests that assert the specific behavior described (exact substring absence; exact route-path set equality, not just a count), the deletion is surgical (`git diff` shows nothing else touched), and the full suite is green at the expected count of 8. The conftest baseline MT5 patch from Subphase 1.2 was empirically re-verified (not just assumed) to still be working and load-bearing for the collection-time import hazard it targets. One framing note (not a gap): the plan's acceptance criteria are satisfied by a function-scoped import rather than a true module-level one, so the "first module-level import" milestone that will really stress-test this guard under normal test execution is still ahead, in Subphase 4.2/4.3 (which add genuine module-level `TestClient(app)` construction) — worth re-running an equivalent empirical check at that point rather than assuming this pass carries forward automatically. Ready for the developer to continue to Subphase 1.5.

**pr-reviewer follow-ups, resolved in this same branch:**
1. `test_router_has_exactly_ten_active_routes` used `{route.path for route in router.routes}` set equality, which can't detect a duplicate registration on an existing path inflating `len(router.routes)` while the path-set stays the same size. Added an explicit `len(router.routes) == len(expected_paths)` assertion alongside the path-set comparison.
2. `test_breakout_strategy_removed`'s `read_text()` had no explicit encoding (platform-codec risk on non-ASCII bytes). Matched the sibling hygiene tests' `encoding="utf-8", errors="ignore"` convention.
3. **Scope widened**: the deleted comment block was the only caller of `app.utils.backtest_signals.backtest_signals` (confirmed via repo-wide grep), leaving an unused import in `endpoints.py` and a fully-dead module that no later subphase was scheduled to catch. Removed the import and deleted `app/utils/backtest_signals.py`; added `test_backtest_signals_module_removed` to cover it.

### 2026-09-12 — Subphase 1.5 (Remove unused `rm` import from endpoints.py)

Checked: Subphase 1.5 only.

- `pipenv run pytest -q` (full suite) → 11 passed (the prior 8 plus 2 new ones in `tests/routes/test_endpoints.py`: `test_endpoints_import_succeeds`, `test_rm_import_removed`). `--collect-only` confirms exactly these 11, nothing extra picked up. Matches the plan's expected count.
- `git diff app/routes/endpoints.py` shows a clean, single-line removal: only `rm,` is gone from the `from app.factory import (...)` block; nothing else in the file changed.
- **Was `rm` genuinely dead before the change?** Confirmed via `git show HEAD:app/routes/endpoints.py` plus a regex scan (`\brm\b`) over that pre-change text: exactly one match, the import line itself (`    rm,`). No route handler in the file ever referenced `rm` as a bare identifier. `rm` is `app.factory`'s module-level `RiskManager` singleton (`rm = create_risk_manager(br)`); it's still used elsewhere in `app/factory.py` (passed into `create_trade_executor`/`create_enter_trade`/`create_exit_trade`) and is still exported for Subphase 4.2's future `Depends()` wiring — this subphase only removes the now-pointless re-import into `endpoints.py`, not the singleton itself. Confirmed the same regex scan over the post-change file: zero matches anywhere (not even inside a comment or string), i.e. the identifier is fully gone from this file, not just from the import line.
- **Is the `\brm\b` regex itself a correct, robust check?** Yes. `\b` requires a word/non-word-character boundary on each side, so `\brm\b` matches only a standalone `rm` token (e.g. `rm,`, `rm =`, `(rm)`) and cannot false-positive on substrings like `firm`, `perform`, `warm`, `orchestrators`, or `term` (in each, the character adjacent to `r`/`m` is itself a word character, so no boundary exists there). It also does not miss the real import line's exact spelling (`    rm,` — matched, confirmed above). Manually checked the current file for any other 2-character `rm` substring, standalone or not — none exist, so the test isn't accidentally trivial in a way that would hide a bug; it's a precise, non-brittle check for exactly the identifier in question.
- `test_rm_import_removed` reads the file's live source and asserts `not re.search(r'\brm\b', source)` — matches the second acceptance criterion exactly.
- `test_endpoints_import_succeeds` does `from app.routes import endpoints` and asserts `hasattr(endpoints, 'router')` and `hasattr(endpoints, 'get_status')` — satisfies the first acceptance criterion ("test asserts `import app.routes.endpoints` still succeeds") with a slightly stronger check (also confirms the module's public surface didn't silently break).
- **Does `test_endpoints_import_succeeds` genuinely exercise `app.factory`'s module-level MT5 calls via `mock_mt5`, as its own docstring claims?** Checked this empirically rather than assuming. `--collect-only` shows test order within the file is: `test_breakout_strategy_removed`, `test_router_has_exactly_ten_active_routes` (already does `from app.routes.endpoints import router` at test-body scope, using `mock_mt5`), `test_backtest_signals_module_removed`, `test_endpoints_import_succeeds`, `test_rm_import_removed` — all in one pytest process. Verified via a standalone script (`import app.routes.endpoints` once inside a `mock.patch` context covering `mt5.initialize`/etc., then, with the patches removed so `mt5.initialize` is back to the real MT5 SDK function, importing `app.routes.endpoints` a second time) that a second import of an already-imported module is a pure `sys.modules` cache hit — it does not re-run `app.factory`'s module-level code, and the second import succeeded even with real (unmocked) `mt5.initialize` in place, returning the identical module object (`is` check passed). Applying that to the actual suite: `test_router_has_exactly_ten_active_routes` is what performs the one genuine, MT5-touching import of `app.routes.endpoints`/`app.factory` in this file (protected by both its own `mock_mt5` fixture and the unconditional conftest baseline patch); by the time `test_endpoints_import_succeeds` runs afterward, both modules are already cached, so its `from app.routes import endpoints` is a cache lookup, not a real module-level MT5-touching import — its `mock_mt5` fixture argument does no protective work in this specific test as currently ordered, despite the docstring's "ensures... doesn't break the module" framing implying a fresh exercise.
- This is **not a functional gap** — nothing is broken, the suite is genuinely protected (by `test_router_has_exactly_ten_active_routes` + the conftest baseline patch, both already QA'd in Subphase 1.2/1.4) and no test touches real MT5 — but it is the same class of caveat Subphase 1.2's QA flagged and deferred ("re-run an equivalent empirical check... rather than assuming it carries forward automatically"): this specific test's use of `mock_mt5` is decorative, not load-bearing, given current file ordering. Worth keeping in mind for Subphase 4.3, where genuine module-level `TestClient(app)` construction is added — that's the point to re-verify this isn't papering over a real caching-order hazard.
- No test calls real MT5 or a real broker; the full suite remains green at 11.

**Verdict: proceed.** Both stated acceptance criteria are met by tests that assert the specific behavior described. `rm` was confirmed genuinely dead in `endpoints.py` both before (exactly one match: the import line) and after (zero matches) the change, and the `\brm\b` regex is a correct, robust check with no false-positive/false-negative risk from substrings. One non-blocking observation: `test_endpoints_import_succeeds`'s `mock_mt5` fixture is currently decorative (the module is already cached by an earlier test in the file), so it doesn't "genuinely exercise" a fresh module-level MT5 call the way its docstring implies — not a bug, but worth re-checking once Subphase 4.3 adds real module-level `TestClient` construction. Ready for the developer to continue to Subphase 1.6.

**pr-reviewer follow-ups, resolved in this same branch:** `app/factory.py`'s trailing comment ("Export orchestrators dict, trade_executor, br, rm, md for endpoints") was now stale since `rm` is no longer imported into `endpoints.py` — no later subphase was scheduled to catch it (1.7 only covers CLAUDE.md/README.md), so fixed it here. Also removed a pre-existing unused `import pytest` in `tests/routes/test_endpoints.py`.

### 2026-09-12 — Subphase 1.6 (Move the root MT5 smoke-test script out of the pytest-lookalike name)

Checked: Subphase 1.6 only.

- `pipenv run pytest -q` (full suite) → 14 passed (the prior 11 plus 3 new ones in `tests/test_repo_hygiene.py`: `test_mt5_smoke_test_moved`, `test_no_stray_test_files_in_repo_root`, `test_mt5_reference_updated_in_claude_md`). `--collect-only` confirms exactly these 14, nothing extra picked up — matches the plan's expected count.
- **Real move, not a rewrite:** confirmed `test_mt5.py` is gone from the repo root (`Path("test_mt5.py").exists()` is `False`) and `scripts/mt5_smoke_test.py` exists. Extracted the last-committed content of `test_mt5.py` via `git show HEAD:test_mt5.py` and diffed it against the new `scripts/mt5_smoke_test.py`: `diff -u` reports zero differences, both files are exactly 490 bytes, and MD5 checksums match exactly (`924316ea1b3af10f3be336a903d14a0a` for both). This is a byte-identical move, not a rewrite.
- **Non-recursive stray-test-file check:** `test_no_stray_test_files_in_repo_root` uses `REPO_ROOT.iterdir()` (non-recursive — only immediate children) filtered to `item.is_file()` and a `test_*.py` name match. This is genuinely non-recursive as the plan specifies: it never descends into `tests/` or `scripts/` at all (rather than recursing and then having to exclude those directories by name), so there's no risk of it accidentally flagging `tests/test_repo_hygiene.py`, `tests/test_main.py`, etc. Verified empirically by temporarily creating a throwaway `tests/test_zz_probe.py` and `scripts/test_zz_probe.py` and re-running this one test: it still passed (both ignored, as expected since `iterdir()` doesn't recurse into subdirectories), then deleted both probe files and re-confirmed the suite is back to 14 passed.
- **CLAUDE.md reference:** `git diff CLAUDE.md` shows a single-line, surgical change — `` `test_mt5.py` at the repo root is a separate manual MT5 connectivity smoke script `` → `` `scripts/mt5_smoke_test.py` is a separate manual MT5 connectivity smoke script `` — nothing else in the file touched. `test_mt5_reference_updated_in_claude_md` asserts both the absence of `test_mt5.py` and the presence of `scripts/mt5_smoke_test.py` in `CLAUDE.md`'s text — matches the third acceptance criterion exactly.
- **Repo-wide staleness check:** grepped the whole repo (excluding `.git/`) for the substring `test_mt5`. Only expected hits remain: this plan file itself (describing the change, including the already-historical Subphase 1.2 QA note that correctly says "not yet moved — that's Subphase 1.6"), and the new assertions in `tests/test_repo_hygiene.py`. No other source, config, or doc file references the old path.
- No test in this subphase calls real MT5 or a broker — all three new tests are pure filesystem/text checks, consistent with the rest of `test_repo_hygiene.py`.
- Noted for the record (not a gap): `scripts/` is currently untracked in git status (`?? scripts/`) since this subphase hasn't been committed yet — this is expected mid-subphase state, not a defect.

**Verdict: proceed.** All three stated acceptance criteria are met by tests that assert the specific behavior described, not weaker checks. The move is confirmed byte-identical via diff and checksum (a real move, not a rewrite), `test_mt5.py` is confirmed gone from the repo root, the stray-file check is genuinely non-recursive by construction (no reliance on excluding `tests/`/`scripts/` by name since it never recurses), and `CLAUDE.md` was updated with a single surgical edit with no stale `test_mt5.py` references left anywhere in the repo. Full suite green at the expected count of 14. Ready for the developer to continue to Subphase 1.7.

**pr-reviewer finding, resolved in this same branch (significant — undermined this subphase's own stated goal):** the chosen name `scripts/mt5_smoke_test.py` still matches pytest's default `python_files` glob (`*_test.py`) — `pytest.ini` only overrides `testpaths`, not `python_files`. Verified empirically: `pytest scripts/` or any explicit path pointing at it would execute the script's body (real `mt5.initialize()`, `symbol_select()` which mutates Market Watch, `positions_get()`) during collection. `just test`/bare `pipenv run pytest` are shielded by `testpaths = tests`, so the risk was latent rather than active, but it defeats the subphase's title ("move ... out of the pytest-lookalike name") — a plan-level naming miss, not a developer error, so fixed directly rather than deferred to Subphase 1.7. Renamed to `scripts/mt5_smoke.py` (matches neither `test_*.py` nor `*_test.py`), updated `CLAUDE.md` and both test assertions accordingly, and widened `test_no_stray_test_files_in_repo_root` to also catch `*_test.py`-style names at the repo root, since criterion 2's original `test_*.py`-only check would not have caught the very file this subphase created. Also removed the bare `except OSError: pass` around `REPO_ROOT.iterdir()` (a second, minor pr-review nit) — a real iteration failure now propagates instead of the test passing vacuously. Confirmed via `pytest scripts/ --collect-only`: "no tests collected" for the renamed file.

The QA entry above's "`--collect-only` confirms exactly these 14, nothing extra picked up" is only accurate for the bare `pytest` invocation shielded by `testpaths` — it does not hold once an explicit path is passed (as the finding above demonstrates). Correcting the record here rather than editing QA's own entry.

### 2026-09-12 — Subphase 1.7 (Fix stale documentation references) + Phase 1 close-out

Checked: Subphase 1.7, plus a broader repo-wide Phase 1 staleness sweep (all of 1.1–1.7).

**Subphase 1.7 itself:**

- `pipenv run pytest -q` (full suite) → 16 passed (the prior 14 plus 2 new ones in `tests/test_repo_hygiene.py`: `test_entry_filter_removed_from_docs`, `test_main3_removed_from_docs`). Matches the plan's expected count exactly.
- `git diff CLAUDE.md` shows exactly two surgical one-line changes: the indicators list in the "Signal generation" paragraph drops `, entry_filter.py)` → `)`, and the API-layer paragraph's `app/main3.py` clause changes from "is a separate/alternate entrypoint — check which one is current before assuming `app.main:app` is the only target" to "was removed in this sweep and `app.main:app` is the sole entrypoint." Nothing else in the file touched.
- `git diff README.md` shows exactly one surgical one-line change: "SMA, MACD, RSI, entry filter calculations" → "SMA, MACD, RSI calculations" in the Project Structure section. Nothing else touched.
- `test_entry_filter_removed_from_docs` asserts the exact substring `entry_filter.py` is absent from both `CLAUDE.md` and `README.md` — matches the first acceptance criterion exactly. `test_main3_removed_from_docs` asserts the exact substring `app/main3.py is a separate/alternate entrypoint` is absent from `CLAUDE.md` — matches the second acceptance criterion exactly (both are precise substring checks, not weaker structural checks, but the criteria as written only call for substring absence, so this is a correct match, not a shortcut).
- Repo-wide grep for `entry_filter.py`: zero hits outside the two new test assertions (which name the string as the thing being checked for, not a live reference) and this plan file's own prose. Repo-wide grep for `main3`: the only remaining hit is `CLAUDE.md`'s now-corrected sentence itself (expected — it's the fixed reference, not a stale one) plus this plan file and the test suite's assertions.
- **Sanity check per instruction:** confirmed `app/signals/indicators/entry_filter.py` was correctly left alone — it does not exist in the working tree, and `git log --all --diff-filter=A -- app/signals/indicators/entry_filter.py` returns zero commits, meaning this file has never existed anywhere in this repo's git history. The `CLAUDE.md`/`README.md` references were always aspirational/stale documentation for a file that was never actually created, not a reference to something the developer deleted out of scope. `app/signals/indicators/` currently contains exactly `__init__.py`, `macd.py`, `rsi.py`, `sma_crossover.py` — unchanged by this subphase.
- Checked the two other repo-wide hits for the substring `entry_filter` (`app/signals/strategies/strong_signal_strategy.py`, `app/signals/strategies/multi_timeframe.py`): both are an unrelated keyword-argument name, `apply_entry_filters: bool = False`, on `generate_signal()` — not a reference to the `entry_filter.py` file path, and outside this subphase's scope. No action needed.
- No test in this subphase calls real MT5 or a broker — both new tests are pure filesystem/text reads, consistent with the rest of `test_repo_hygiene.py`.

**Subphase 1.7 verdict: proceed.** Both stated acceptance criteria are met by tests that assert the specific substrings described, both doc edits are surgical (`git diff` shows nothing else touched), the out-of-scope sanity check confirms `entry_filter.py` was never a real file and remains correctly untouched, and the full suite is green at the expected count of 16.

**Phase 1 close-out sweep (broader check, since this is the last subphase of Phase 1):**

Re-ran `pipenv run pytest -q` → 16 passed, confirming the full suite is green at the end of Phase 1. Then grepped the whole repo (excluding `docs/plans/` and the QA/plan text itself) for every stale-reference pattern the individual Phase 1 subphases were supposed to clean up:

- `loss copy`/`profit copy`/`sma_crossover copy` (Subphase 1.1 backup files): zero hits anywhere outside `tests/test_repo_hygiene.py`'s assertions and this plan file. Files confirmed absent from the filesystem.
- `main3` / `utils.connection` / `utils/connection` (Subphase 1.2): the only remaining hit is `CLAUDE.md`'s corrected sentence (expected, not stale) plus test assertions and this plan file. `app/main3.py` and `app/utils/connection.py` confirmed absent from the filesystem.
- `process_handling` (Subphase 1.3): zero hits anywhere outside test assertions and this plan file. `app/utils/process_handling.py` confirmed absent.
- `BreakoutStrategy` / `backtest_signals` (Subphase 1.4): only hit is `tests/routes/test_endpoints.py` (the assertions checking for their absence/removal) and this plan file. `app/utils/backtest_signals.py` confirmed absent; no `BreakoutStrategy` reference anywhere in `app/`.
- Bare `rm` import (Subphase 1.5): `\brm\b` regex over `app/routes/endpoints.py` finds zero matches.
- `test_mt5.py` at repo root (Subphase 1.6): confirmed absent; `scripts/mt5_smoke.py` present; no `test_*.py`/`*_test.py`-named file exists anywhere in the repo root's immediate children.
- `entry_filter.py` / `app/main3.py is a separate/alternate entrypoint` (Subphase 1.7): confirmed clean, per above.

`git status --short` shows only the three expected modified files for this subphase (`CLAUDE.md`, `README.md`, `tests/test_repo_hygiene.py`) — no stray untracked files, no leftover backup/probe artifacts from any earlier subphase's QA process.

**Phase 1 complete.** All seven subphases (1.1–1.7) have been verified individually and, with this close-out sweep, cross-checked against each other for stale references left behind. The full test suite passes at 16/16, no test touches real MT5 or a broker, every deletion target confirmed absent from the filesystem, and no dangling reference to any deleted/moved file remains anywhere in the repo outside of this plan's own historical narrative and the tests that assert the cleanup. Per the branch's instructions, Phase 1 being done does not mean the overall plan is done — `docs/plans/in-progress/project-refactor-sweep.md` stays in `in-progress/` until Phase 5 finishes. Ready for the developer to proceed to Subphase 2.1.

**pr-reviewer finding, resolved in this same branch (2 of the 3 new test assertions were vacuous):** `test_main3_removed_from_docs` checked for the substring `app/main3.py is a separate/alternate entrypoint`, but CLAUDE.md always wrapped the path in backticks (`` `app/main3.py` is a separate/alternate entrypoint ``) — the assertion passed against the *pre-change* file too, confirmed empirically via `git show master:CLAUDE.md`. Likewise, the README half of `test_entry_filter_removed_from_docs` checked for `entry_filter.py`, but README's actual text was "entry filter calculations" (space, no `.py`) — that assertion also passed against master unchanged. Both were genuine-looking guards that verified nothing; this QA entry's earlier claim that they were "precise substring checks, not weaker structural checks" was wrong for these two and is superseded here. Fixed: the CLAUDE.md check now asserts `"is a separate/alternate entrypoint" not in content` (no backtick-dependent phrase) plus a positive check that `"sole entrypoint"` is present (mirroring the existing pattern in `test_mt5_reference_updated_in_claude_md`); the README check now asserts `"entry filter" not in readme_content` (no `.py` suffix assumption). Re-verified all four assertions individually fail against `git show master:CLAUDE.md`/`README.md` and pass against the current working tree — genuine regression guards now, not before-and-after coincidences. Root cause: the plan's own acceptance-criteria wording (line 54) assumed a `.py`-suffixed, unbacktick'd exact phrase without checking the docs' actual text — the same class of miss as the Subphase 1.6 `*_test.py` naming defect, a plan-level gap rather than a developer error.

The open question at plan line 146 (whether `main3.py`'s `/ws/symbol_data`/`/account_info` functionality should be ported) is still unresolved and was not implicitly closed by this subphase's CLAUDE.md wording change — flagging again here since pr-review noted the new "sole entrypoint" phrasing could read as though it settled the question. It has not; the plan should still be revisited on this point before or shortly after reaching `docs/plans/done/`.

### 2026-09-12 — Subphase 2.1 (Fix the broken `/close_all` endpoint) — Phase 2 elevated triage

**`pipenv run pytest -q` → 19 passed** (up from 16 at Phase 1 close-out; +3 for the two new `tests/trade_execution/test_trade_execution.py` tests and the one new `tests/routes/test_endpoints.py` test). No skips, no warnings surfaced.

**Implementation (`app/trade_execution/trade_execution.py::TradeExecutor.close_all_trades`, lines 288–299) matches the plan's Change bullet exactly:**
```python
def close_all_trades(self):
    positions = self.broker.get_open_positions()
    for pos in positions or []:
        ticket = pos_ticket(pos)
        if ticket is not None:
            self.broker.close_position(ticket=ticket)
```
- `pos_ticket` is imported at module top (`from app.exit_strategies.exit_shared import pos_ticket`, line 8) — correct source, and it's the real dict/object-safe accessor (`get_any(position, ("ticket", "id", "position", "order"))` in `exit_shared.py`), not a local reimplementation.
- No `try/except` anywhere in the method — a `close_position` failure for one ticket will propagate rather than being silently swallowed, and there's nothing else in the method that could swallow an exception either. Matches "no silent exception swallowing."
- `positions or []` guards a `None`/falsy return from `get_open_positions()` so the loop doesn't blow up on an empty book — reasonable defensive addition, not required by the plan but not a deviation from it.
- None-ticket skip is a plain `if ticket is not None:` guard — correctly skips without raising, and does not call `close_position` at all for that position (not even with a `None` ticket kwarg).
- Endpoint (`app/routes/endpoints.py` lines 72–75) now calls `trade_executor.close_all_trades()` — the old nonexistent `trade_executor._close_all_trades()` call is gone.

**Repo-wide grep for `_close_all_trades`** (the old broken method name) turns up zero hits anywhere in `app/`, `tests/`, or elsewhere — only the two new test *function names* (`test_close_all_trades_...`, which reference the new public method, not the old private one) match a substring search for `close_all_trades`. No dangling caller of the old name remains.

**Test coverage, verified by reading the actual test bodies (not just names):**
- `test_close_all_trades_closes_all_open_positions` (`tests/trade_execution/test_trade_execution.py:8`): sets `mock_broker.get_open_positions.return_value = [make_position(as_dict=True, ticket=1), make_position(as_dict=True, ticket=2)]`, constructs a real `TradeExecutor` with `mock_broker` and `MagicMock()` for the other two collaborators, calls `close_all_trades()`, then asserts `mock_broker.close_position.call_count == 2` plus `assert_any_call(ticket=1)` / `assert_any_call(ticket=2)`. This is a genuine, precise assertion of the exact acceptance criterion in plan line 61 — not a "ran without error" smoke test. (Minor nit, not a gap: lines 39–40 build an unused `tickets_closed` set via a convoluted `call.kwargs.get(...) or call[1].get(...) if ... else ...` expression that is never asserted on — dead code left over from drafting, superseded by the two `assert_any_call` lines directly below it. Harmless, but worth a cleanup pass; not blocking.)
- `test_close_all_trades_skips_positions_with_no_ticket` (`tests/trade_execution/test_trade_execution.py:47`): sets `get_open_positions.return_value = [make_position(as_dict=True, ticket=None)]`, calls `close_all_trades()`, asserts `mock_broker.close_position.assert_not_called()`. This is a real, independently-run test (not a claimed-but-absent case) and it genuinely exercises the None-ticket skip path — confirmed by tracing `pos_ticket` on a dict with an explicit `ticket: None` key: `get_any` finds the key present and returns `None` (not falling through to `id`/`position`/`order`), so the method's `if ticket is not None` guard is the thing actually under test, not an accessor fallback quirk. This test was not explicitly required by the plan's stated acceptance criteria (plan line 61 only specifies the two-ticket-closed case) but is a welcome addition given this is money-handling logic — no gap here, a strict improvement over the plan's minimum bar.
- `test_close_all_endpoint_calls_trade_executor_method` (`tests/routes/test_endpoints.py:103`): builds a fresh `FastAPI()` app, includes the real `router`, patches `app.routes.endpoints.trade_executor` with a `MagicMock()` via `unittest.mock.patch`, POSTs `/close_all` through a `TestClient`, and asserts `response.status_code == 200` and `mock_trade_executor.close_all_trades.call_count == 1`. Because the patch target is the module-level `trade_executor` name that the endpoint function closes over, and the endpoint body is literally `trade_executor.close_all_trades()`, this test can only pass if the endpoint calls the new method by that exact name — it would fail loudly (`AttributeError` surfaced as a 500, or `call_count == 0`) against the old `_close_all_trades()` call. Confirmed this is testing the new code path, not a stale/broken one.

**No real MT5/broker contact:** `mock_broker` (from `tests/conftest.py`) is a bare `MagicMock()` with a few attrs pre-set (`mode`, `open_positions_sim`, `close_position.return_value`); no network or MT5 terminal calls anywhere in it. The endpoint test additionally takes the `mock_mt5` fixture (defensive, given `app.routes.endpoints` imports `app.factory` at module level which eagerly constructs a real `Broker`) and separately patches `trade_executor` itself, so `close_all_trades()` never reaches a real broker even indirectly. Consistent with every prior subphase's MT5-safety pattern in this plan.

**Verdict: proceed.** Implementation matches the plan's Change bullet exactly, both stated acceptance-criteria tests exist and assert the precise described behavior (not just "runs without error"), the None-ticket edge case this codebase cares about is genuinely covered (bonus, not required), correct `pos_ticket` import/usage, and a repo-wide grep confirms no remaining caller of the old nonexistent `_close_all_trades`. Cosmetic nit (dead `tickets_closed` computation) removed in-branch before pr-review.

**pr-reviewer findings (high-effort review, since this touches real trade-closing logic), resolved in this same branch:**

1. **Abort-on-first-error (significant):** the original `close_all_trades()` had no `try/except` around the per-position `close_position()` call. Since `close_position` can raise (e.g. `AttributeError` if `mt5.order_send` returns `None`, per `broker.py`), a single bad position would abort the entire "close everything" panic-button call, leaving every subsequent position open with no indication of which ones. Fixed: each ticket is now closed inside its own `try/except`, failures (raised exceptions or an explicit `False` return) are collected rather than propagated, and the method returns `{"closed": [...], "failed": [{"ticket", "error"}, ...]}` instead of `None`. Added `test_close_all_trades_continues_after_one_failure` proving all positions are still attempted when one raises and one returns `False`.
2. **Endpoint always reported success:** `/close_all` returned a hardcoded `{"status": "all trades closed"}` regardless of what actually happened. Fixed: the endpoint now returns the real `close_all_trades()` result plus a `status` field that reflects whether anything failed (`"all trades closed"` vs `"some trades failed to close"`). Added `test_close_all_endpoint_reports_failures` and updated the original endpoint test to assert on the response body, not just the status code.
3. **Undocumented scope (explicit decision made, not silently left):** `get_open_positions()` is unfiltered by symbol or `Config.MAGIC_NUMBER`, so this closes *every* position on the account, including ones opened manually or by another EA. This is now an explicit, documented decision rather than an accidental gap: the endpoint is literally named `/close_all` / `close_all_trades`, and narrowing it to bot-only positions would silently change what a panic button does without being asked to. Documented in the method's docstring. If bot-only scoping is ever wanted, that should be its own deliberate change with its own name (e.g. `close_all_bot_trades`), not folded into this one.
4. **Shadowed import (latent trap, fixed defensively):** `execute_exit`'s local loop variable was also named `pos_ticket`, shadowing the module-level `pos_ticket` import added by this subphase. No live bug today (the local is assigned before any read in that method), but it would turn a future refactor of that method into an `UnboundLocalError` trap. Renamed the local to `candidate_ticket`.
5. **Demo-mode no-op (already tracked, no action needed here):** confirmed `close_all_trades()` closes nothing in demo/backtest mode today, since simulated positions have no `ticket` assigned until Subphase 2.5 lands — expected, not a regression introduced by this subphase.

Full suite green at 21 (was 19; +2 for the new failure-handling tests). Clear to proceed to Subphase 2.2.

### 2026-09-12 — Subphase 2.2 (Fix `is_break_even()` to support dict-shaped positions) — Phase 2 elevated triage

**`pipenv run pytest -q` → 26 passed** (up from 21; +5 new tests in `tests/exit_strategies/test_exit_shared.py`). Matches the expected count.

**Diff is exactly the one-line change described, nothing else touched:** `git diff HEAD -- app/exit_strategies/exit_shared.py` shows only:
```python
 def is_break_even(position) -> bool:
-    profit = getattr(position, "profit", None)
+    profit = pos_profit(position)
     if profit is None:
         profit = 0.0
     return profit >= 0.0
```
`git status --porcelain` confirms `app/exit_strategies/exit_shared.py` is the only modified tracked file; `tests/exit_strategies/` is new/untracked. Specifically confirmed `app/exit_strategies/managers/profit.py` and `app/exit_strategies/managers/loss.py` (Subphases 2.3/2.4) are untouched — neither appears in `git status` or `git diff`.

**`pos_profit()` is a genuine dict/object-safe accessor, read directly from `exit_shared.py`:** `pos_profit(position)` calls `get_any(position, ("profit", "pnl", "floating_profit"))`, and `get_any()` branches explicitly on `isinstance(obj, dict)` — dict branch does key lookup (`k in obj`), object branch does `hasattr`/`getattr`. This is materially different from bare `getattr(position, "profit", None)`, which on a `dict` always returns the default (dicts have no `.profit` attribute) regardless of what the dict actually contains.

**Proved the behavior change empirically, not just by reading code** — `git stash`'d the change, ran the old code directly:
```
OLD: is_break_even({"profit": -5.0}) -> True   (wrong — reported break-even while at a $5 loss)
OLD: is_break_even({"profit": 0.0})  -> True
OLD: is_break_even({})               -> True
```
`git stash pop`'d back and re-ran on the new code:
```
NEW: is_break_even({"profit": -5.0}) -> False  (correct)
NEW: is_break_even({"profit": 0.0})  -> True
NEW: is_break_even({})               -> True
```
Confirms the fix genuinely changes behavior for the exact case the subphase exists to fix, and leaves the zero/missing-profit cases unchanged as required.

**All 4 stated acceptance-criteria assertions are present and correct**, verified by reading the test bodies in `tests/exit_strategies/test_exit_shared.py`:
1. `is_break_even({"profit": -5.0})` is `False` — `test_is_break_even_dict_negative_profit`. ✓
2. `is_break_even({"profit": 0.0})` is `True` — `test_is_break_even_dict_zero_profit`. ✓
3. `is_break_even(make_position(profit=-5.0))` (object form) is `False` — `test_is_break_even_object_negative_profit(make_position)`; `make_position`'s default `as_dict=False` returns a `FakePosition` object (plain `setattr`-per-kwarg), not a dict, so this genuinely exercises the object branch of `get_any`. ✓
4. `is_break_even({})` is `True` — `test_is_break_even_dict_missing_profit`, preserves the documented "missing profit defaults to break-even" behavior. ✓

**Extra case beyond the plan:** `test_is_break_even_dict_positive_profit` (`{"profit": 5.0}` → `True`) was added but not required by the plan's acceptance criteria. This is a beneficial addition, not scope creep — it's a real, independently-meaningful case (profit strictly positive, distinct from the exactly-zero case already covered) and costs nothing to keep.

**Other callers of `is_break_even()` — checked whether their behavior changes, and whether that's correct:** repo-wide grep finds exactly two other call sites, both in Phase 2's remaining subphases: `app/exit_strategies/managers/profit.py:86` (`ProfitExitManager.check_exit_on_tick`) and `app/exit_strategies/managers/loss.py:73` (`LossExitManager.check_exit_on_tick`). Note for the record, not a gap in this subphase: both manager files still contain their own separate, unfixed `profit = getattr(position, "profit", None)` (line 96 in `profit.py`, line 46 in `loss.py`) for the actual profit-tracking/exit-threshold logic downstream of the `is_break_even()` check — those are exactly Subphases 2.3 and 2.4's targets and are correctly left untouched here.

**Correction (pr-reviewer, high-effort review) — the claim above that these call sites "are reached with dict-shaped positions in demo/backtest mode" is not accurate today.** Verified: `_simulate_trade`/`_backtest_trade` (`app/trade_execution/broker.py`) key the constructed dict as `"lot"`, but `pos_volume()` looks for `("volume", "lots", "qty", "quantity")` — no match, so both managers' `volume is None` guard bails before either ever reaches the `is_break_even()` call this subphase fixed. Separately, simulated `"profit"` is hardcoded `0.0` at creation and never updated anywhere in `app/` (no mark-to-market loop exists — already correctly noted in this plan's "Out of scope" section as a future feature, not a bug this sweep fixes). **Net effect: this subphase's fix is currently inert in production** — correct and required, but not yet observable, because the dict-position path that would exercise it doesn't actually reach these managers yet. Subphase 2.5 has been amended to fix the `lot`/`volume` key mismatch so this fix (and 2.3/2.4's) actually becomes reachable.

**More important finding from the same review, now driving a plan change:** the "strict improvement, not a new bug" framing above is also not safe once 2.4 lands. `LossExitManager`'s arming window (`state.be_arming_ticks < be_arming_ticks`) has a commented-out `failed_to_reach_be` exit — with `is_break_even()` now honest, a position that never reaches break-even within the window falls out of block 1, never enters block 2 (`be_armed` never set), and gets no exit at all on any subsequent tick, however deep the loss grows. The old always-`True` bug was accidentally providing a backstop (immediate arming meant block 2's drop-after-BE logic was always live) that this fix removes without something to replace it. **Subphase 2.4 has been amended** to also restore the `failed_to_reach_be` safety net as a required part of that subphase, not deferred — see its updated Change bullet and second acceptance-criteria test above. This subphase (2.2) itself ships as originally scoped; the gap it exposes is Subphase 2.4's responsibility to close before Phase 2 is considered done, not this one's.

**No real MT5/broker contact:** all 5 new tests call `is_break_even()` directly with plain dicts or the `make_position` fixture's `FakePosition` — no broker, no MT5 import, no network.

**Verdict: proceed.** The change is exactly the one-line fix the plan specifies, confirmed via diff and via empirical old-vs-new behavior comparison (not just trusting the developer's description). All 4 required acceptance-criteria assertions exist and are correct; the one extra test is a harmless, genuine addition. The two other callers of `is_break_even()` are correctly affected by this fix in the intended direction (less wrong, not newly wrong), and the remaining bugs in those same files are explicitly out of scope here (Subphases 2.3/2.4). Full suite green at 26/26. Clear to proceed to Subphase 2.3.

### 2026-09-12 — Subphase 2.3 (Fix `ProfitExitManager` to read profit via the dict/object-safe accessor) — Phase 2 elevated triage

**`pipenv run pytest -q` → 27 passed** (up from 26; +1 new test in `tests/exit_strategies/managers/test_profit.py`). Matches the expected count.

**Diff is exactly the described change, nothing else touched:** `git diff app/exit_strategies/managers/profit.py` shows only the `pos_profit` import added alongside the other `pos_*` imports, and `profit = getattr(position, "profit", None)` → `profit = pos_profit(position)`, with the `if profit is None: profit = 0.0` fallback line left unchanged. No other file under `app/` is modified.

**The developer's claim about the plan's stated acceptance-criteria values is correct — verified by reading the code directly, not by trusting the description.** The plan's literal text calls for `profit=-0.10`, `best_profit=0.0`, expecting `reason="trailing_breach_gt_5c"`. Reading `check_exit_on_tick` (lines 113-122): the only branch that can produce `trailing_breach_gt_5c` is gated by `if 0.00 < profit < state.best_profit:`. With `profit=-0.10`, the first clause `0.00 < profit` is `0.00 < -0.10`, which is `False` — the branch is unreachable **for any value of `state.best_profit`**, since a negative profit can never satisfy `0.00 < profit`. This isn't a subtle edge case; the plan's own numbers cannot trigger the branch it names, regardless of the getattr/pos_profit bug being fixed or not. The developer's adapted values (`profit=0.05`, `state.best_profit=0.10`) do work: `0.00 < 0.05 < 0.10` is `True`, and `state.best_profit - profit = 0.05 > breach_threshold (0.04)` is `True`, so the immediate-exit branch fires with `reason="trailing_breach_gt_5c"` for real. Confirmed by running the suite (27 passed) and by hand-tracing the arithmetic against the actual source.

**Confirmed the test genuinely proves the fix, not just that code runs.** Hand-traced what the *old* buggy code (`getattr(position, "profit", None)`) would have done against this same dict position: `getattr({...}, "profit", None)` returns `None` (a dict has no `.profit` attribute), which the existing fallback turns into `profit = 0.0`. With `profit=0.0` and `state.best_profit=0.10` pre-set: `0.00 < 0.0` is `False` (strict inequality — the boundary case matters here), so the trailing-breach branch is skipped entirely and the method falls to the `else: state.breach_ticks = 0` catch-all and returns `None`. The new test asserts `result is not None` and `result.reason == "trailing_breach_gt_5c"`, which the old code cannot produce for this input. So the test discriminates correctly between "reads the real dict profit" and "silently defaults to 0.0" — it is not a case where both old and new code happen to pass. `pos_profit()` itself was independently re-verified (`get_any(position, ("profit", "pnl", "floating_profit"))`, branching on `isinstance(position, dict)`) and `make_position(as_dict=True, profit=0.05)` was confirmed via `tests/conftest.py` to produce a literal `{"profit": 0.05, ...}` dict, not an object — so the test exercises the real dict-key path, not `FakePosition`'s attribute path.

**No real MT5/broker contact:** the new test uses only `MagicMock()` for `broker`/`dynamic_buffer`/`htf_allows_profit_exit`/`is_favorable_vs_anchor` and plain dict/lambda fixtures — no MT5 import, no network, no real broker.

**Independent finding — a second commented-out safety net, analogous to loss.py's (Subphase 2.2 → 2.4), but materially less severe.** Lines ~128-138 contain a dead, docstring-quoted `elif state.breach_ticks >= breach_tick_limit: return self._exit_action(..., reason="trailing_breach_timeout")` block. As written, once a position is inside the trailing-breach branch (`0.00 < profit < state.best_profit`, i.e. currently profitable but off its peak) and the breach is `<= $0.04` (too small for the immediate-exit branch), `state.breach_ticks` increments forever with no ceiling — the "wait up to 5 ticks for recovery" comment's implied timeout never fires. Assessed independently against the loss.py comparison raised by the main session: **this is not the same class of bug.** The loss.py gap (pre-2.4) left a position that never reached break-even with *zero* exit path while losses grew unbounded — a genuine "loses money with no protection" defect. This gap is bounded on both sides: (1) `profit > 0.00` is required to even be in this branch, so the position is currently profitable, not underwater; (2) the instant the breach exceeds $0.04 below peak, the *unconditional*, already-wired immediate-exit branch fires regardless of `breach_ticks` — so the maximum give-back from peak while stuck in this state is capped at ~$0.04, not unbounded; (3) if profit ever drops to `<= 0.00`, this branch's guard condition stops applying altogether and `LossExitManager` (a separate, independently-armed manager) becomes the relevant safety net instead. So the realistic failure mode is "a still-profitable position can sit indefinitely within a 4-cent pullback band instead of being forced to lock in profit after 5 ticks as the comment intends" — a profit-taking/efficiency gap, not a capital-protection gap. It is a real bug worth fixing (dead code contradicting its own comment, same pattern that caused a real regression in loss.py) but does not block Phase 2 and does not need to be folded into 2.4 or block Phase 2 completion the way the loss.py gap did. Recommend a small follow-up subphase (implement-and-test the `trailing_breach_timeout` branch) rather than an urgent amendment.

**Verdict: proceed.** The one-line fix matches the plan's Change description exactly (confirmed via diff); the developer's discrepancy claim about the plan's own acceptance-criteria numbers is correct on inspection (`profit=-0.10` can never satisfy `0.00 < profit` for any `best_profit`); the adapted test values (`profit=0.05`, `best_profit=0.10`) genuinely trigger the real code path and were shown by hand-tracing to fail under the pre-fix code, so the test actually proves the fix rather than passing vacuously. Full suite green at 27/27, no real MT5/broker contact. The commented-out `trailing_breach_timeout` block is a genuine but lower-severity gap (bounded profit-giveback, not unbounded loss) — flagged here for a future follow-up subphase, not blocking. Clear to proceed to Subphase 2.4.

**pr-reviewer correction (high-effort review) to QA's severity assessment — one supporting argument was wrong, though the overall verdict (non-blocking) still holds.** QA's claim that "if profit drops to ≤0, `LossExitManager` becomes the relevant safety net" is not accurate as of *this* subphase, because `loss.py` still has its own unfixed `getattr(position, "profit", None)` bug (Subphase 2.4's target) — for dict positions today it always reads `0.0`, so none of its exit branches can fire regardless of real profit. `ExitTrade.on_tick` confirmed to run `_loss_manager.check_exit_on_tick` and `_profit_manager.check_exit_on_tick` against the **same shared `PosState` per ticket** every tick (loss checked first, always; profit checked second, only if no loss action fired) — so once Subphase 2.4 lands, a position crossing from positive profit to negative *is* structurally covered by `loss.py`'s own logic, just at a much coarser threshold (`-$5` drop-after-BE vs. `profit.py`'s `$0.04` trailing threshold — a ~125x scale mismatch). Concretely: a position that gives back from `+$0.10` to `-$0.02` crosses out of `profit.py`'s trailing-exit guard (`profit > 0.00` required) into `loss.py`'s territory, but `-0.02` is nowhere near loss.py's `-$5` threshold either — so that specific small give-back currently has **no immediate exit trigger in either manager** even after 2.2/2.3/2.4 all land, only "tracked as unprofitable after BE" until it either recovers into `0 < profit < 0.05` (`be_recovered_after_unprofit`) or drops all the way to `-$5`. This is a real threshold-tuning gap between the two managers, not a code bug — deciding whether to tighten `loss.py`'s post-BE drop threshold, extend `profit.py`'s trailing logic to also cover the negative-crossing case, or leave the two managers' scales as intentionally different requires an explicit strategy decision, not a mechanical fix. Recorded as a new open question below rather than folded into Subphase 2.4 (which is scoped to a clear, unambiguous restoration of already-intended-but-commented-out behavior) or 2.3 (whose own fix is correct and complete as scoped).

### Subphase 2.4 QA (2026-09-13)

**Diff is exactly the two described changes, nothing else touched.** `git diff HEAD -- app/exit_strategies/managers/loss.py` shows only: (1) `pos_profit` added to the import list and `profit = getattr(position, "profit", None)` → `profit = pos_profit(position)`, with the `if profit is None: profit = 0.0` fallback line left unchanged (matches Subphase 2.4's first Change bullet and the pattern already applied in 2.2/2.3); (2) the docstring-quoted `failed_to_reach_be` block uncommented and moved from a nested position (immediately after the `is_break_even` arm-check, inside block 1's `if`) to a new top-level `if not state.be_armed and state.be_arming_ticks >= be_arming_ticks:` block placed immediately after block 1, using the same `reason="failed_to_reach_be"` and `_exit_action(...)` signature already used elsewhere in the method. `git status --porcelain` confirms only `app/exit_strategies/managers/loss.py` (modified) and `tests/exit_strategies/managers/test_loss.py` (untracked) changed; `git diff HEAD -- app/trade_execution/broker.py` is empty — Subphase 2.5's file is untouched, as it should be.

**Full suite: 29/29 passed** (`pipenv run pytest -q`), matching the expected count (27 going into this subphase + 2 new tests here).

**Both new tests are genuine regression guards, verified empirically, not just by trusting green output.** Stashed only the `loss.py` change (kept the new test file in place) and re-ran `tests/exit_strategies/managers/test_loss.py` against the pre-2.4 code: both tests fail — `test_loss_exit_manager_reads_dict_position_profit_and_exits_on_drop` fails because `getattr(dict, "profit", None)` returns `None` → defaults to `0.0`, which never crosses the `-5` threshold; `test_loss_exit_manager_exits_after_arming_window_expires_without_be` fails at `assert result is not None` on the 21st call because the safety net is still a docstring, so the method returns `None` forever once the arming window is exhausted (exactly the unbounded-loss gap Subphase 2.2's QA flagged). Restored the working tree (`git stash pop`) and re-confirmed 29/29 green afterward, so the verification left no residue.

**Hand-traced (by actually running the real `LossExitManager` against a scripted 21-tick sequence, not just reading the assertions) that the arming-window test drives through exactly the right number of ticks with no premature exit:** with `profit=-1.0` fixed every tick (never crosses the `-5` drop threshold, never reaches break-even), calls 1-20 each increment `state.be_arming_ticks` by exactly 1 (1→20) and return `None`, with `state.be_armed` staying `False` throughout — confirming block 1 alone handles the whole window and nothing fires early. Call 21 leaves `state.be_arming_ticks` at 20 (block 1 is now skipped, so the counter stops incrementing) and returns the `failed_to_reach_be` `ExitAction` unconditionally — the new top-level check has no other gating condition once `be_armed` is `False` and the counter has reached the limit, so it cannot fire late or be skipped by some other interaction. The test's per-iteration assertions (`be_arming_ticks == tick_num`, `be_armed is False`) for ticks 1-20, followed by the tick-21 assertion, match this traced behavior exactly. Not vacuous.

**The commented-out-code restructuring nuance (raised by the main session) — confirmed and assessed as non-blocking.** The original commented-out code was nested *inside* block 1, positioned after the `is_break_even` arm-check but before block 1's own `return None`, structured so that on the very tick the counter first reaches the limit (tick 20 in the default-20 case), the same call would fall through to the `failed_to_reach_be` check and exit immediately — same-tick firing. The developer's restructuring instead placed the check as a new top-level `if`, structurally after block 1's own `return None`. Because block 1 unconditionally returns `None` on every call where its guard is true (whether via the drop-profit branch, the BE-arm branch, or its own trailing `return None`), the new top-level check is only ever reached on the *next* call after block 1's guard goes false — i.e., tick 21, not tick 20. This is a real, confirmed one-tick behavioral difference from the literal original nested design, empirically verified above. It is not, however, a deviation from this subphase's actual (amended) spec: the plan's acceptance criteria for Subphase 2.4 explicitly says "assert that the very next call (**tick 21**) returns an `ExitAction`" — the plan was written with this exact off-by-one framing already in mind, so the implementation matches its literal acceptance criteria, not just its spirit.

**Severity assessment of the one-tick delay, as requested:** not meaningfully concerning for this money-handling path, for four reasons. (1) "Ticks" here are price-update events, not fixed time units — the 20-tick arming window is already an approximation of elapsed time with irregular granularity, so one additional tick lengthens the window by ~5% in event-count terms and by however long a single tick interval happens to be in absolute time (typically the smallest time granularity this system observes at all, i.e. sub-second to a few seconds in an actively-ticking market). (2) The extra tick is not an unguarded exposure gap: on tick 21 the new top-level check fires unconditionally the moment it's reached — there's no scenario where it fires later than tick 21 or gets skipped, so the delay is bounded to exactly one tick, never more. (3) The magnitude of risk being closed by this subphase (a position that previously could sit fully unprotected with *unbounded* loss for the rest of its life once `is_break_even()` was fixed in 2.2) dwarfs the magnitude of a single-tick boundary shift; trading a real "never exits" defect for a "exits one tick later than the most literal reading of dead code intended" characteristic is a large net improvement, not a new risk. (4) This exact behavior was already deliberately encoded in the plan's amended acceptance criteria rather than being an accidental developer choice, so it reflects a reviewed decision, not an overlooked regression. No follow-up action recommended; if the team ever wants strict same-tick nested-equivalent firing, that would be a deliberate, separate behavior-change subphase, not a bug fix.

**No real MT5/broker contact:** the new tests use only `MagicMock()` for `broker`/`risk_manager` and plain dict/lambda fixtures (`make_position(as_dict=True, ...)`, `make_tick(as_dict=True, ...)`) — no MT5 import, no network, no real broker calls.

**Verdict: proceed.** Both required changes (profit accessor fix, safety-net restoration) match Subphase 2.4's amended Change bullets and acceptance criteria exactly; both new tests are genuine (shown to fail against pre-fix code, hand-traced against the real implementation, not just passing incidentally); nothing outside `loss.py` and its test file was touched, `broker.py` (Subphase 2.5) is untouched; full suite green at 29/29; no real MT5/broker contact. The one-tick delay between the literal original nested-comment design and the restored top-level-check design is real and confirmed, but matches the plan's own (amended) acceptance criteria and is not a meaningful capital-protection concern given its bounded, single-tick size relative to the unbounded-loss defect it closes. Clear to proceed to Subphase 2.5.

**pr-reviewer correction to this QA entry's characterization (high-effort review) — resolved in this same branch.** QA's framing of the restructuring as *only* a one-tick timing delay was incomplete: hoisting the safety-net check to a separate top-level block also dropped a second, independent piece of the original nested design's behavior — the original checked `is_break_even(position)` *before* deciding to exit (since it lived after the BE-arm branch inside block 1), so a position reaching break-even on the very tick the arming window expires would arm and be handed to `ProfitExitManager`, not get force-closed. The top-level restructuring lost that check entirely: tick 21 would have force-closed an already-profitable position as `failed_to_reach_be`. This was a genuine semantic gap introduced by this subphase (not present before it, and not covered by the plan's stated acceptance criteria), so — unlike the one-tick timing delay, which was a deliberate, reviewed part of the amended spec — this one was fixed directly: added an `is_break_even(position)` recheck inside the new top-level block, arming instead of exiting when it's true. Added `test_loss_exit_manager_arms_instead_of_exiting_if_be_reached_on_deciding_tick`, confirmed to fail without the fix (reverted just `loss.py` via `git stash` and reran — failed with `be_armed` staying `False`) and pass with it. Full suite now green at 30.

**pr-reviewer also flagged a pre-existing, out-of-scope config-wiring bug, recorded as a new open question below rather than fixed here:** `LossExitManager` reads `getattr(self.config, "EXIT_BE_ARMING_TICKS", 20)`, but the `config` object actually passed in production (`ExitTradeConfig`, an all-lowercase-field dataclass) has no such attribute, so the hardcoded fallback of `20` always wins over `settings.py`'s configured `EXIT_BE_ARMING_TICKS = 90` — a real config value the operator can never actually change. Before this subphase this was inert (the counter only gated the `-5` drop check, itself duplicated at the same threshold in block 2); after this subphase, it decides when the newly-restored safety net force-closes a position — 4.5x sooner than configured. `profit.py`'s `be_distance_pips` has the same class of defect against `EXIT_BE_DISTANCE_PIPS`. Not fixed here since it's a pre-existing, separate bug outside this subphase's diff and deserves its own subphase (which should also add a `be_arming_ticks > 0` guard, since a naive fix wiring the real config through would make `EXIT_BE_ARMING_TICKS = 0` force-close every position on tick 1).
